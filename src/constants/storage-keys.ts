/** Keys used with persist / localStorage (single FE bundle, no API). */
export const STORAGE_KEYS = {
  TRIP_STATE: 'trip-planner-pro/trips',
  CALENDAR_EVENTS: 'trip-planner-pro/calendar-events',
} as const
